import { useEffect, useState } from "react";
import { Route, Switch } from "react-router-dom";

const MyAccount = () => {
  // const [user, setUser] = useState("");

  // useEffect(() => {
  //   fetch(`https://nc-marketplace.herokuapp.com/api/users/${username}`)
  //     .then((response) => response.json())
  //     .then((data) => {
  //       setUser(data.user);
  //     });
  // }, [username]);

  // if (user) {
  // }

  return (
    <section>
      <p>My Account</p>
    </section>
  );
};

export default MyAccount;
